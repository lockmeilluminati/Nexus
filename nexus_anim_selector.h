#ifndef NEXUS_ANIM_SELECTOR_H
#define NEXUS_ANIM_SELECTOR_H

#include "nexus_core.h"
#include "nexus_timeline.h"

// Renders the dropdown and updates the timeline track internally
void DrawAnimationSelector(SceneObject& selObj, TimelineTrack& selTrack);

#endif
#pragma once
#include "nexus_core.h" // Points to the master struct definition!
#include <string>

// Orchestrates uniform asset allocation rules across external scripts
SceneObject SpawnCharacter(const std::string& filePath);